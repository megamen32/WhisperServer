# Install as a whisperserver.service.d drop-in to opt into Russian-first ASR.
# Explicit whisper-1 requests keep their existing Whisper model semantics.
[Service]
EnvironmentFile=/home/roomhacker/services/whisperserver-defaults.env

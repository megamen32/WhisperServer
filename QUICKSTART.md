# Quickstart

Актуальный быстрый старт перенесён в [docs/quickstart.md](docs/quickstart.md).

```bash
git clone https://github.com/megamen32/WhisperServer.git
cd WhisperServer
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```
